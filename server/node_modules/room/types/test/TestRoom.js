var util = require('util');
var MilkRoom = require('room/MilkRoom');

var data = require('./RoomData');

function TestRoom(id) {
	MilkRoom.call(this, id, data.type, data.capacity, data.spawnPoints);
}

util.inherits(TestRoom, MilkRoom);

module.exports = TestRoom;