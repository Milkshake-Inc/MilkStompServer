var util = require('util');

var AbstractRoom = require('room/AbstractRoom');

function MilkRoom(id, type, capacity, spawnPoints) {
	AbstractRoom.call(this, id, type, capacity);

	this.spawnPoints = spawnPoints;
}

util.inherits(MilkRoom, AbstractRoom);

module.exports = MilkRoom;